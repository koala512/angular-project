import { Product } from './product.model';

export class CartItems {
  // public id: string;

  // constructor(
  //   public product: Product,
  //   public price: number,
  //   public variant: keyof Product['variants'],
  // ) {
  //   this.id = CartItems.generateId(variant, product.id);
  //   this.product = product;
  //   this.price = price;
  // }
  // public static generateId(variant: keyof Product['variants'], id: string) {
  //   return `${id}::${variant}`;
  // }
}
